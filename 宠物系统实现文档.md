# 宠物系统实现文档

## 📋 概述

已完成宠物系统的核心实现，包括：
- ✅ 完整的实体模型（宠物模板、玩家宠物、技能等）
- ✅ 改进的个体值（IV）系统
- ✅ 经验值和升级机制
- ✅ 技能学习和装备系统
- ✅ 宠物队伍管理
- ✅ 初始数据（10只宠物 + 27个技能）

## 🏗️ 项目结构

```
src/main/java/com/yourname/petbattle/
├── enums/                  # 枚举类型
│   ├── PetType.java       # 宠物属性类型（火/水/草/电/土/龙）
│   ├── SkillType.java     # 技能类型（物理/魔法/变化）
│   ├── PetStatus.java     # 宠物状态（正常/烧伤/中毒/麻痹等）
│   ├── PetRarity.java     # 稀有度（普通/优秀/稀有/史诗/传说）
│   └── StatType.java      # 属性类型（HP/攻击/防御等）
│
├── entity/                # 实体类
│   ├── PetTemplate.java   # 宠物模板（所有玩家共享的基础数据）
│   ├── PlayerPet.java     # 玩家宠物实例
│   ├── Skill.java         # 技能
│   ├── PetLevelSkill.java # 宠物升级技能关联
│   ├── PlayerPetSkill.java # 玩家宠物已学会的技能
│   └── PlayerTeam.java    # 玩家队伍
│
├── repository/            # 数据访问层
│   ├── PetTemplateRepository.java
│   ├── PlayerPetRepository.java
│   ├── SkillRepository.java
│   ├── PetLevelSkillRepository.java
│   ├── PlayerPetSkillRepository.java
│   └── PlayerTeamRepository.java
│
├── service/              # 业务逻辑层
│   ├── PetGrowthService.java      # 宠物成长（经验值、升级、属性计算）
│   ├── SkillLearnService.java     # 技能学习
│   └── PetManagementService.java  # 宠物管理（获取、队伍管理）
│
├── controller/           # 控制器
│   └── PetController.java         # 宠物API接口
│
├── dto/                  # 数据传输对象
│   ├── ApiResponse.java
│   ├── PetCreateRequest.java
│   └── AddExpRequest.java
│
├── config/               # 配置
│   └── DataInitializer.java      # 数据初始化器
│
└── PetBattleApplication.java     # 主应用类
```

## 🎮 核心功能

### 1. 宠物数据模型

#### 宠物模板（PetTemplate）
```java
- 基础属性：HP、攻击、防御、魔攻、魔抗、速度
- 成长率：每级增加的属性值
- 稀有度：1-5星
- 属性类型：火/水/草/电/土/龙
```

#### 玩家宠物实例（PlayerPet）
```java
- 等级系统：1-100级
- 经验值：基于等级的三次方计算
- 个体值（IV）：每项属性独立的0-31随机值
- 计算后的最终属性
- 当前状态：正常/烧伤/中毒等
```

### 2. 属性计算公式

```
最终属性 = 基础值 + (成长率 × 等级) + IV
HP额外有10%加成
```

示例（5级火焰犬，HP的IV为25）：
```
最大HP = (50 + 3.0×5 + 25) × 1.1 = 99
```

### 3. 经验值系统

```java
// 升到N级所需总经验 = N³
等级1 → 2: 需要 8 经验
等级2 → 3: 需要 27 经验
等级10 → 11: 需要 1331 经验
```

### 4. 技能系统

#### 技能分类
- **物理技能**：使用攻击和防御计算伤害
- **魔法技能**：使用魔攻和魔抗计算伤害
- **变化技能**：不造成伤害，施加状态效果

#### 技能学习机制
- 宠物升级时自动学会对应等级的技能
- 最多装备4个技能
- 可以学习更多技能但需要替换才能装备

### 5. 属性相克系统

已在PetType枚举中实现：
```java
火 > 草 > 水 > 火   (基础循环，伤害×1.5)
电 > 水            (克制，伤害×1.5)
龙 > 龙            (互克，伤害×1.5)
土 > 电           (克制，伤害×1.5)
被克制             (伤害×0.5)
```

## 🗄️ 数据库设计

### 核心表结构

```sql
-- 宠物模板表
pet_template (id, name, type, rarity, base_hp, base_attack, ...)

-- 玩家宠物实例表
player_pet (id, player_id, pet_template_id, level, exp, iv_hp, iv_attack, ...)

-- 技能表
skill (id, name, skill_type, pet_type, power, accuracy, max_pp, ...)

-- 宠物升级技能关联表
pet_level_skill (id, pet_template_id, skill_id, learn_level)

-- 玩家宠物已学会的技能
player_pet_skill (id, player_pet_id, skill_id, current_pp, position, is_equipped)

-- 玩家队伍表
player_team (id, player_id, position, player_pet_id, is_active)
```

## 📊 初始数据

### 初始宠物（可选作为起始宠物）
1. **火焰犬**（火系/稀有）- 速度型
2. **水灵龟**（水系/稀有）- 防御型
3. **青叶蛇**（草系/稀有）- 平衡型

### 其他宠物
4. 雷电鼠（电系/优秀）- 极速型
5. 岩石怪（土系/优秀）- 坦克型
6. 幼龙（龙系/史诗）- 全能型
7. 炎魔（火系/史诗）- 魔攻特化
8. 海皇（水系/史诗）- 生存特化
9. 森林之王（草系/史诗）- 均衡强化
10. 雷神（电系/传说）- 速度+魔攻极致

### 技能示例
- 火系：火花、火焰喷射、火焰旋涡、炽热冲锋
- 水系：水枪、水炮、冰冻光线、水之波动
- 草系：藤鞭、飞叶快刀、寄生种子、阳光烈焰
- 电系：电击、十万伏特、打雷、电光一闪（先制+1）
- 土系：落石、地震、挖洞
- 龙系：龙之怒、龙爪、龙之波动、逆鳞
- 通用：撞击、叫声、睡觉

## 🔌 API接口

### 宠物管理

```http
# 获取所有初始宠物
GET /api/pets/starters

# 选择初始宠物
POST /api/pets/choose-starter
{
  "playerId": 1,
  "petTemplateId": 1,
  "nickname": "小火"
}

# 获取玩家的所有宠物
GET /api/pets/player/{playerId}

# 增加经验值
POST /api/pets/add-exp
{
  "playerPetId": 1,
  "exp": 100
}
```

### 技能管理

```http
# 获取宠物已学会的技能
GET /api/pets/{petId}/skills

# 获取宠物已装备的技能
GET /api/pets/{petId}/skills/equipped

# 装备技能到指定位置
POST /api/pets/{petId}/skills/{skillId}/equip?position=0

# 卸下技能
POST /api/pets/{petId}/skills/{skillId}/unequip
```

### 治疗

```http
# 治疗单个宠物
POST /api/pets/{petId}/heal

# 治疗玩家所有宠物
POST /api/pets/player/{playerId}/heal-all
```

## 🚀 启动步骤

### 1. 环境准备

确保已安装：
- Java 17+
- Maven 3.6+
- MySQL 8.0+
- Redis（可选，用于后续匹配系统）

### 2. 数据库配置

创建数据库：
```sql
CREATE DATABASE pet_battle CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

修改 `application.yml` 中的数据库配置：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_battle?...
    username: your_username
    password: your_password
```

### 3. 启动应用

```bash
# 编译项目
mvn clean compile

# 运行应用
mvn spring-boot:run
```

应用启动后：
- 自动创建数据库表（JPA DDL auto）
- 自动初始化基础数据（DataInitializer）
- 访问 http://localhost:8080

### 4. 测试API

使用Postman或curl测试：

```bash
# 1. 获取初始宠物列表
curl http://localhost:8080/api/pets/starters

# 2. 选择初始宠物（假设火焰犬ID为1）
curl -X POST http://localhost:8080/api/pets/choose-starter \
  -H "Content-Type: application/json" \
  -d '{"playerId":1,"petTemplateId":1,"nickname":"小火"}'

# 3. 查看玩家宠物
curl http://localhost:8080/api/pets/player/1

# 4. 增加经验值（假设宠物ID为1）
curl -X POST http://localhost:8080/api/pets/add-exp \
  -H "Content-Type: application/json" \
  -d '{"playerPetId":1,"exp":200}'

# 5. 查看宠物技能
curl http://localhost:8080/api/pets/1/skills/equipped
```

## ✨ 核心特性

### 1. 个体值系统
每只宠物的个体值完全随机（0-31），即使是同一模板的宠物，属性也会有差异，增加养成乐趣。

### 2. 自动技能学习
宠物升级时自动学会对应等级的技能，无需手动操作。

### 3. 灵活的技能装备
- 可以学习多个技能
- 只能装备4个技能用于战斗
- 支持随时更换装备的技能

### 4. 数据驱动设计
所有宠物和技能数据都在DataInitializer中定义，方便调整平衡性。

## 📈 后续扩展

### MVP阶段暂未实现
1. **努力值（EV）系统** - 战斗获得，单项最大252，总和最大510
2. **宠物捕捉系统** - 野外遭遇、捕捉球机制
3. **宠物进化系统** - 特定等级或条件触发
4. **道具系统** - 恢复药、增益道具、进化石等

### 可以直接实现
1. **对战系统** - 使用现有的属性和技能数据
2. **匹配系统** - 使用Redis队列
3. **排行榜** - 基于ELO评分

## 🔧 关键代码说明

### 属性计算
`PetGrowthService.calculateStat()` - 根据模板、等级、IV计算最终属性

### 升级逻辑
`PetGrowthService.addExp()` - 增加经验值，可能触发连续升级

### 技能学习
`SkillLearnService.checkAndLearnSkillsOnLevelUp()` - 升级时自动学习技能

### 初始宠物选择
`PetManagementService.chooseStarterPet()` - 创建5级宠物，学习初始技能

## 🎯 设计改进点

相比原需求文档的改进：

1. ✅ **完善了个体值系统** - 各属性独立IV，而非单一值
2. ✅ **增加了技能管理表** - player_pet_skill，支持学习多个技能
3. ✅ **实现了经验值系统** - exp和next_level_exp字段
4. ✅ **增加了宠物稀有度** - 1-5星分级
5. ✅ **提供了初始宠物机制** - is_starter标记
6. ✅ **添加了队伍管理** - player_team表

## 📝 注意事项

1. **数据库自动建表**：使用JPA的`ddl-auto: update`，首次启动会自动创建表
2. **数据初始化**：DataInitializer只在数据库为空时执行
3. **Redis可选**：当前实现的功能不依赖Redis，Redis用于后续的匹配系统
4. **日志级别**：开发环境设置为DEBUG，可以看到所有SQL语句

## 🐛 常见问题

**Q: 启动时报数据库连接错误？**
A: 检查MySQL是否启动，用户名密码是否正确

**Q: 数据没有初始化？**
A: 检查日志，DataInitializer会在数据已存在时跳过

**Q: 升级后技能没有自动学会？**
A: 检查pet_level_skill表是否有对应配置

## 📚 下一步计划

建议按以下顺序继续开发：

1. **用户系统**（1-2天）
   - 用户注册登录
   - JWT认证
   - 用户基础信息

2. **对战系统**（5-7天）
   - 战斗引擎核心逻辑
   - 伤害计算实现
   - 状态效果处理
   - WebSocket实时通信

3. **匹配系统**（2-3天）
   - Redis匹配队列
   - ELO评分系统
   - 匹配算法

4. **数据持久化**（1天）
   - 战斗记录保存
   - 战绩统计

5. **前端开发**（10-14天）
   - Vue 3 + TypeScript
   - 对战UI界面
   - 宠物展示和管理

---

**当前进度**：✅ Phase 1完成 - 宠物系统核心实现
**代码行数**：约2500行
**测试覆盖**：需补充单元测试
**文档完整度**：90%
